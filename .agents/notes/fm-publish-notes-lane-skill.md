# Task notes: fm-publish-notes-lane-skill

Durable project-intrinsic knowledge this task produced.
The fold routes each fact below into the destination document and then retires this file.
Keep the bar high: knowledge useful to almost every future session, and a pointer to the authoritative file or command rather than detail the code already shows.

## Destination

AGENTS.md

## Notes

- `skills/firstmate-notes/` is now the public installer-facing home of the note-lane convention (`SKILL.md`, `fm-notes.sh`, `test/run.sh`, copied from that source and then diverged as the entries below record). The grown local copy at `~/.claude/skills/firstmate-notes/` on this machine is untouched and remains the live, loaded copy for this fleet; nothing repoints to the published copy. Retiring the local copy in favor of an installed one is a separate future task, not done here.
- Scoping this publish required trimming `~/.claude/skills/firstmate-notes/SKILL.md`'s "Why this exists, measured" section, whose rationale was measured from the captain's own projects' merge history - captain ruling 2026-08-14 excluded that project history as material unrelated to what the skill is published for. It stays in firstmate's own private records; a generic one-line rationale replaced it in the published `SKILL.md`. Also generalized the sentence describing firstmate's own gitignored local memory as the thing that reminds an agent to load the skill, and dropped two smaller fleet-internal asides (the "dispatched by firstmate" phrasing, and a historical note about a since-removed in-repo concurrent-fold guard). `scan`'s `FM_HOME` mechanism and the rest of the doc were left untouched by design - only the identified fleet-internal or captain-private sentences were rewritten.
- Captain-private material does not enter any tracked file, and a note under `.agents/notes/` is no exception. The note lane exists to carry durable knowledge *into* the repo, which makes a note the last place private material may sit, not a safe holding pen for it. Record that an exclusion happened and on whose authority; keep the excluded material itself in firstmate's own private records.
- Destination judgment call for the fold to decide, not resolved here: neither `AGENTS.md` (checked all of it, including section 13's agent-only-skill trigger list) nor `CONTRIBUTING.md` currently mentions `firstmate-notes` or the note lane anywhere - grepped the whole tracked tree, zero hits before this task. So this publish does not make any existing pointer stale; there was none to begin with. Whether the fold wants to *add* a trigger/pointer now that an installable copy exists is a separate decision, not something this task's ruling asked for.
- `bin/fm-test-run.sh`'s `families_for_changed_path` had no case for top-level `skills/*`, so `--changed` died on this task's own new files. This was the first public skill to add executable scripts under `skills/` (`skills/stow` is prose-only), which is why nothing had hit it before. Fixed by adding `skills/*` to the existing no-family ignore-list case alongside `docs/*` and `.agents/notes/*`: a public skill carries its own self-contained tests, run directly rather than through this suite, and is never referenced by `tests/*.test.sh`. Anything else ever published into `skills/` hits the same gap unless it lands under that same case.
- `bin/fm-lint.sh`'s canonical set now covers every `*.sh` at any depth under `skills/`, not just the historical `bin/*.sh bin/backends/*.sh tests/*.sh`. Newly published skill scripts otherwise shipped with no shellcheck at all: a skill owns scripts at its own depth (`skills/<name>/test/run.sh`), so the match is recursive on purpose and anything published into `skills/` later is covered without another edit. Both the full-set path and the changed-mode predicate `fm_lint_is_canonical_root` were widened together, and `tests/fm-lint.test.sh` pins both. Consequence to keep in mind when publishing a skill: its scripts must also survive CI's stock macOS Bash 3.2 `-n` parse sweep, which iterates `bin/fm-lint.sh --list-files`.
- The published `skills/firstmate-notes/` is not a copy of the source at `~/.claude/skills/firstmate-notes/` and must not be described as one. It has diverged in three kinds of way: a comment-level shellcheck suppression, the behavior of `scan`'s explicit-directory form, and the test coverage that pins that behavior. Do not trust any count or enumeration of the difference, here or anywhere else - every such list in this note has gone stale within a round. The authoritative and always-current statement of it is what a reader can run: `diff -u ~/.claude/skills/firstmate-notes/fm-notes.sh skills/firstmate-notes/fm-notes.sh` and `diff -u ~/.claude/skills/firstmate-notes/test/run.sh skills/firstmate-notes/test/run.sh`. Anyone closing the gap later must port the tests along with the behavior, not the behavior alone.
- The suppression is a `# shellcheck disable=SC2016` comment above `test/run.sh`'s `contains()` helper, on a verified false positive ruled by firstmate after reading the line: the single quotes sit inside an already double-quoted string, so `$2` and `$1` do expand as intended. It exists because this repo lints published skill scripts (previous entry) while the source copy is linted by nothing.
- The behavior change is in `scan`'s explicit-directory form. It now resolves each named directory through `resolve_dir` the way `path`/`new`/`list`/`count` already do, reports a path it cannot read instead of skipping it, and holds each resolved worktree root once. Before, a typo'd path, a plain file, or a subdirectory instead of the worktree root all counted nothing and exited `0`, indistinguishable from a genuinely clear lane; and once resolution was added, a repo named twice (as itself and as its own subdirectory) was counted twice. `scan` now has three outcomes on three distinct exit codes - `3` pending, `0` clear, `1` could-not-look - and the dedupe sits at the resolution boundary, in the one function that admits a root into the scanned set, so nothing downstream can count or print a repo twice. The source copy is deliberately untouched and still carries both bugs; closing that gap is firstmate's, not this task's.
- That `scan` bug predates this publish and was never reachable in practice while the skill had one user and one invocation (`FM_HOME`, whose targets are constructed rather than typed). It surfaced only because publishing forced `SKILL.md` to document the explicit-directory form for readers with no firstmate home, and review then checked whether the documented exit contract was true for that form. Publishing a grown local tool is what audits it: the contract it had always claimed was false for the audience it was about to acquire.
- README's "Two-tier skill layout" section no longer describes what each public skill needs or does inline - across three review rounds on this task, first a stale enumeration, then an over-broad capability claim, then the same claim still incomplete, all failed the same way. It now only names each skill under `skills/` and points to that skill's own `SKILL.md` as the one owner of its own contract. Keep it that way when a third public skill is added: append the name, not a description.
